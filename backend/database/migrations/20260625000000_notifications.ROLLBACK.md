# Rollback: 20260625000000_notifications

## Summary

Drops the `notifications` and `notification_preferences` tables and the `NotificationEvent` enum type.

## Steps

```sql
DROP TABLE IF EXISTS notification_preferences;
DROP TABLE IF EXISTS notifications;
DROP TYPE IF EXISTS "NotificationEvent";
```

## Verification

```sql
SELECT table_name FROM information_schema.tables
WHERE table_name IN ('notifications', 'notification_preferences');
-- Should return 0 rows

SELECT typname FROM pg_type WHERE typname = 'NotificationEvent';
-- Should return 0 rows
```
